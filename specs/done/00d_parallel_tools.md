Currently tools all run sequentially

This is extremely slow especially for web fetches!!!

We should run web searches as well as web fetches on different domains in parallel as
each step.

Other tools are extremely fast (normally) so they can stay sequential.
