- [x] Better file watcher logs
- [x] Deleting file should delete embeddings
- [x] _Actual_ batching of embeddings since we already batch file changes, but it seems
      to me embeddings go file by file?
- [ ] ghost cli -> can we access it when daemon is running? I think surrealdb is
      blocking database access with daemon running? What would be the simplest solution
      to fix this?
