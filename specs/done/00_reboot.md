Need to add /reboot discord command:

- Starts a new session (fresh system prompt and messages history)
- Runs reflection on the old one if it didn't trigger yet (or will it be automatic on
  next tick?)
