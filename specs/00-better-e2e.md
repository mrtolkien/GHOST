Need to review the full e2e testing flow:

- One test per step
- Save output for next test -> keep in repo, needs to be simple to review
- One option to run them all sequentially
- Should be easy to add new e2e tests -> dioxus docs import + query test!
- Should be easy to run it for multiple models
