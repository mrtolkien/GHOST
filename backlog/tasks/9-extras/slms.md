I'd want to use SLMs or find a good way to have information about:

- Non-seqitur = messages that would benefit from a fresh session
- Routing = evaluates task complexity and routes to LLM with the right "power"
  - Kinda moot with prompt caching which requires re-hitting the same
