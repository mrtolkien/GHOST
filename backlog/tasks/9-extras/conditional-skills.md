- Make skills conditional on env variables?
  - For example image generation depends on gemini API key

Or we could make the other argument: the skills _teach_ how to set themselves up, so
leaving them lets the user configure them _then_ use them
