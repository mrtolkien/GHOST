Docling and crawl4ai can be used as Python modules.

They also are packaged like shit, their nix pkgs/containers crash half the time, some of
the features are missing, ...

What if we went the nano banana root: instead of a tool or container, we have a python
script associated to a skill? Then we could use uv to run the python script even with
dependencies. And the GHOST would be able to create new/better scripts, using all of the
features of those services.
