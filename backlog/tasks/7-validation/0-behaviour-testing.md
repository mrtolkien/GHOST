For _each_ skill, tool, and agent, feature (project, coding agent), we should do
behaviour testing the same way Claude does it with its skill creator:

- 10 prompts that should trigger, 10 prompts that should not
- check for conciseness/number of tool calls, ...
