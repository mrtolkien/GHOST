Important concept:

Notes are TRUE information. They are not time-sensitive, they don't expire, ...

This is crucial:

- If the GHOST notices a false note, it should update or delete it. Updating should
  clearly say it is WRONG and why it's kept (history of decisions for example)
- Diary _can_ hold "false" information: for example old design choices that have been
  superseeded
- Example of proper information: OPERATOR was born in 1989
- Example of improper information: OPERATOR is 36 years old

---

Other remark: the GHOST creates notes a bit too often atm:

- Creates them only from web searches, without fetches -> a bit iffy, but maybe ok?
- Creates very low-impact ones like ennemies in Slay the Spire 2, developers names, ...
- Creates them to _reinforce its own conclusions without evidence_ (cf "assets" naming)

We should make it clear that notes are there for knowledge _that was not part of the
model_, which means it 99% of the time comes from web searches and fetches.

I'll attach my current GHOST workspace so you can look at it

---

Extra question: How to store information about the OPERATOR?

It should not all be in the OPERATOR file: the OPERATOR file is _core_, _always-on_
information.

For secondary information, we should have a structured guideline.

---

Cache levels concept: OpenViking (https://github.com/volcengine/OpenViking) is a great
inspiration.

It clearely separates _levels_ of context: L0, L1, L2!

From my understanding:

- L0 is always-on (our 3 core files)
- L1 is important info (our notes?)
- L2 is secondary (references?)

---

Goes hand in hand with archetypes backlog, it's about solidifying the knowledge system
